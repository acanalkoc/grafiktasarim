# Runtime and test dependencies for Scientific Graph Studio.
# Minimum versions are compatible lower bounds (not a host freeze) known to
# support the APIs this project uses; newer compatible releases are fine.
numpy>=1.24
matplotlib>=3.7
pandas>=2.0
openpyxl>=3.1
xlrd>=2.0
scipy>=1.10
pillow>=10.0
pywin32>=306; sys_platform == "win32"
